#include <stdio.h>
#include <stdbool.h>
/*Codigo que explica como funciona el if*/
int main(){

    if(true){
        puts("Todo este ");
        puts("Codigo,se imprime dentro ");
        puts("del bloque if ");
    }
    return 0;
}